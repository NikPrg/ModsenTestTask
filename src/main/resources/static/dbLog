create TABLE Events
 (   id bigint generated by default as identity primary key,
     subject varchar (50) not null,
     description text not null,
     organizer varchar (30) not null,
     place varchar (30) not null,
     event_time timestamp not null,
     created_at timestamp not null
                              );



# Database
db.driver: org.postgresql.Driver
db.url: jdbc:postgresql://localhost:5432/modsen_db
db.username: postgres2
db.password: admin

# Hibernate
hibernate.dialect:


databaseChangeLog:
  - changeSet:
      id: create-events
      author: Nikita
      changes:
        - createTable:
            tableName: events
            columns:
              - column:
                  name: id
                  type: bigint
                  autoIncrement: true
                  constraints:
                    primaryKey: true
                    nullable: false

              - column:
                  name: subject
                  type: varchar(50)
                  constraints:
                    nullable: false

              - column:
                  name: description
                  type: text
                  constraints:
                    nullable: false

              - column:
                  name: organizator
                  type: varchar(30)
                  constraints:
                    nullable: false

              - column:
                  name: place
                  type: varchar(30)
                  constraints:
                    nullable: false

              - column:
                  name: event_time
                  type: timestamp
                  constraints:
                    nullable: false

              - column:
                  name: created_at
                  type: timestamp
                  constraints:
                    nullable: false
